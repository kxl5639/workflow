import tkinter as tk
from tkinter import ttk
from project.project_model import Project, field_metadata, session # type: ignore


# window = tk.Tk()
# window.title("Test")
# frame = ttk.Frame(window, padding="1 1 1 1")
# frame.grid(row=0, column=0, padx=0, pady=0)




#test_tree = create_tree(window,['project_number', 'submittal_date', 'client', 'scope'],session,Project)
#test_tree.grid(row=1, column=0, pady=10, padx=10)

